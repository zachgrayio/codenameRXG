buildscript {
   ext.kotlin_version = '1.0.0'

   repositories {
     mavenCentral()
   }

   dependencies {
     classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
   }
}

apply plugin 'kotlin'sourceSets {
    main.kotlin.srcDirs += 'src/main/myKotlin'

sourceSets {
    main.kotlin.srcDirs += 'src/main/rxg/kotlin'
}
